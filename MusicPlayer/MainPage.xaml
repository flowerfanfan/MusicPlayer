<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MusicPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Schema="using:System.ComponentModel.DataAnnotations.Schema"
    x:Class="MusicPlayer.MainPage"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                     
        <Grid x:Name="MenuGrid"  >
            <!--这个自适应想让窗口宽度小于800时收进去，然而并没有效果。。-->
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="VisualStateGroup">
                    <VisualState x:Name="VisualStateMin0">
                        <VisualState.Setters>
                            <Setter Target="MenuList.IsPaneOpen" Value="false"/>
                        </VisualState.Setters>
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="1"/>
                        </VisualState.StateTriggers>
                    </VisualState>
                    <VisualState x:Name="VisualStateMin800">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="800"/>
                        </VisualState.StateTriggers>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
            
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <RelativePanel>
                <Button x:Name="HamburgerButton" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" FontSize="20" Click="HamburgerButton_Click"  />
            </RelativePanel>
            <!--这是汉堡菜单-->
            <SplitView x:Name="MenuList" Grid.Row="1" DisplayMode="CompactInline" 
                       OpenPaneLength="300"
                       IsPaneOpen="True"
                       CompactPaneLength="40" 
                       HorizontalAlignment="Left">
                <SplitView.Pane>
                    <ListBox SelectionMode="Single" x:Name="IconListBox" SelectionChanged="IconListBox_SelectionChanged">
                        <!--下面是五个菜单项-->
                        <ListBoxItem x:Name="SearchItem" Width="300" Background="White">
                            <StackPanel Orientation="Horizontal" Margin="-2">
                                <Image Source="Assets/search.png" Height="30" Width="30" HorizontalAlignment="left"/>
                                <TextBox x:Name="Search" Width="220" Height="35" HorizontalAlignment="Left" FontSize="16" Text="搜索"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem x:Name="PerviousItem" Width="300" Background="White">
                            <StackPanel Orientation="Horizontal" Margin="-2">
                                <Image Source="Assets/time.png" Height="30" Width="30" HorizontalAlignment="Left"/>
                                <TextBlock Text="最近播放" Height="20"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem x:Name="FavoriteItem" Width="300" Background="White">
                            <StackPanel Orientation="Horizontal" Margin="-2">
                                <Image Source="Assets/heart.png" Height="30" Width="30" HorizontalAlignment="Left"/>
                                <TextBlock Text="我的最爱" Height="20"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem x:Name="SongsItem" Width="300" Background="White">
                            <StackPanel Orientation="Horizontal"  Margin="-2">
                                <Image Source="Assets/music2.png" Height="30" Width="30" HorizontalAlignment="Left"/>
                                <TextBlock Text="我的歌单" Height="20"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem x:Name="ListItem" Width="300" Background="White" >
                            <StackPanel Orientation="Horizontal" Margin="-2">
                                <Image Source="Assets/list.png" Height="30" Width="30" HorizontalAlignment="Left"/>
                                <TextBlock Text="播放列表" Height="20" Width="200"/>
                                <AppBarButton Icon="Add" Background="White" Width="50" Height="46" HorizontalAlignment="Right"/>
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </SplitView.Pane>
                <SplitView.Content>
                    <Grid Width="auto">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="300"/>
                            <RowDefinition Height="1"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0">
                                <Image Source="Assets/singer.png" Width="250" Height="250" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                                <TextBlock Text="歌曲" FontSize="20" VerticalAlignment="Bottom"/>
                            </Grid>

                            <StackPanel Grid.Column="1">
                                <TextBlock Text="专辑名" FontSize="35"/>
                                <TextBlock Text="标签：" FontSize="20" />
                                <TextBlock Text="简介：" FontSize="20"/>
                            </StackPanel>

                            <Grid Grid.Column="2" Height="300" VerticalAlignment="Top">
                                <AppBarButton Icon="Add" VerticalAlignment="Bottom" Width="40" Height="40"/>
                            </Grid>
                        </Grid>
                        <Grid Grid.Row="1">
                            <Line Grid.Column="2" Stretch="Fill" Stroke="Black" StrokeThickness="2" X1="1" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
                        </Grid>
                        <Grid Grid.Row="2">
                            <TextBlock Text="你还没有添加歌曲" FontSize="25"/>
                        </Grid>
                    </Grid>
                </SplitView.Content>
            </SplitView>
        </Grid>

    </Grid>

    <!--底部菜单栏，貌似不能在BottomAppBar中添加Slider还有图片。-->
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Name="VolumeButton" Icon="Volume" Label="Volume">
                <AppBarButton.Flyout>
                    <Flyout>
                        <Slider Orientation="Vertical" Height="100" HorizontalAlignment="Center"/>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
            <AppBarButton x:Name="PreviousButton" Icon="Previous" Label="Previous Song"/>
            <AppBarButton x:Name="PlayButton" Icon="Play" Label="Play"/>
            <AppBarButton x:Name="PauseButton" Icon="Pause" Label="Pause"/>
            <AppBarButton x:Name="NextButton" Icon="Next" Label="Next Song"/>
            <AppBarSeparator/>
            <AppBarButton x:Name="ShareButton" Icon="Link" Label="Share"/>
        </CommandBar>
    </Page.BottomAppBar>


</Page>
